/** Shared client + server — no Next.js server APIs. */
export const DEFAULT_GROUP = "Groupe 5";
export const SELECTED_PLAYER_COOKIE = "dc_selected_player_id";
/** Browser localStorage key — only the selected player id (no match data). */
export const LOCAL_SELECTED_PLAYER_KEY = SELECTED_PLAYER_COOKIE;
